# 栈 stack
**先进后出**（LIFO）  
限定仅在表尾进行插入或删除操作的线性表。  
表尾端称为栈顶（top），表头端称为栈底（bottom）  
### 栈的顺序结构-初始化
    #define MAXSIZE=100
    typedef int ElemType;

    typedef struct{
        ElemType data[MAXSIZE];
        int top;
    }Stack;

    void initStack(Stack *s)
    {
        s->top = -1;   //栈顶指针为-1，表明此时是空栈
    }
### 栈的顺序结构-进栈/压栈
    int push(Stack *s,ElemType e)
    {
      if(s->top >= MAXSIZE-1)
      {
        printf("满了\n");
        return 0;
      }
      s->top++;
      s->data[s->top] = e;  //拿top当下标
      return 1;
    }
### 栈的顺序结构-出栈
    ElemType pop(Stack *s,ElemType *e)
    {
      if(s->top==-1){
        printf("空的\n");
        return 0;
      }
      *e = s->data[s->top];
      s->top--;
      return 1;
    }
### 栈的顺序结构-获取栈顶元素
     int getTop(Stack *s, ElemType *e)
     {
       if(s->top==-1){
        printf("空的\n");
        return 0;
      }
      *e = s->data[s->top];
      return 1;
    }
### 栈的顺序结构初始化-动态内存分配
    #define MAXSIZE 100
    typedef int ElemType;
    typedef struct{
      ElemType *data;
      int top;
    }Stack;
    Stack* initStack(){
      Stack *s = (Stack*)malloc(sizeof(Stack));
      s->data = (ElemType*)malloc(sizeof(ElemType) * MAXSIZE);
      s->top = -1;
      return s;
    }
### 栈的链式结构实现
把链表的头结点作为栈顶指针，方便获取。当然也可以用链表的尾结点作为栈顶指针，但都需要遍历获得尾结点 

     typedef int ElemType;

     typedef struct stack{
      ElemType data;
      struct satck *next;
    }Stack;
### 栈的链式结构-进栈/压栈
    //头插法
    int push(Stack *s,ElemType e)
    {
      Stack *p = (Stack*)malloc(sizeof(Stack));
      p->data = e;
      p->next = s->next;
      s->next = p;
      return 1;
    }
### 栈的链式结构-出栈
    int pop(Stack *s,ElemType *e)
    {
      if(s->next == NULL)
      {
        printf("空的\n);
        return 0;
      }
      *e = s->next->data;
      Stack *q = s->next;
      s->next = q->next;
      free(q);
      return 1;
    }
### 栈的链式结构-获取栈顶元素
    int getTop(Stack *s,ElemType *e)
    {
      if(s-> == NULL)
      {
        printf("空的\n);
        return 0;
      }
      *e = s->next->data;
      return 1;
    }
~~~
//实例验证
int main(int argc,char const *argv[]) {
   Stack s;
   initStack(&s);
   push(&s,10);
   push(&s,20);
   push(&s,30);
   ElemType e;
   pop(&s,&e);
   printf("%d\n",e);
   getTop(&s,&e);
   printf("%d\n",e);
   
   return 0;
}
//输出30,20
~~~
