<img width="253" height="188" alt="image" src="https://github.com/user-attachments/assets/38502d59-a0e9-4107-a06f-07e06619a553" />

# 查找的基本概念
<img width="256" height="167" alt="image" src="https://github.com/user-attachments/assets/83c6dc96-836c-4aaf-8dbe-3a93d7f512c9" />
<img width="257" height="153" alt="image" src="https://github.com/user-attachments/assets/23cc4333-b395-47f6-b1d1-fcbf47ad46aa" />

# 线性表的查找
一、顺序查找（线性查找）  
二、折半查找（二分或对分查找）  
三、分块查找  
<img width="284" height="170" alt="image" src="https://github.com/user-attachments/assets/c3bdfd27-4386-4d83-8182-5c0454300358" />

## 顺序查找
应用范围：  
1.顺序表或线性链表表示的静态查找表  
2.表内元素之间无序  

***代码如下***   
*1.search函数*

    #include<stdio.h>

    int search(int* data,int len,int value){
        for(int i = 0;i < len;i++){
            if(data[i] == value){
                return i;
            }
        }
        return -1;
    }

*2.main函数*

    int main(int argc,char const *argv[]){
        int arr[] = {15,54,76,6,9};
        int len = sizeof(arr) / sizeof(arr[0]);
        int pos = search(arr,len,54);
        printf("%d\n",pos);
        return 0;
    }

**改进：把待查关键字key存入表头（“哨兵”），从后向前逐个比较可免去查找过程中每一步都要检测是否查找完毕，加快速度。**  
***代码如下***  

    //在顺序表L中查找值为e的数据元素
    int search(SSTable ST,KeyType key){
        ST.R[0].key = key;                              //key存入顺序表表头
        for(i = ST.length;ST.R[i].key != key;--i);      //不用检测是否查找完毕，只用比较是否匹配
        return i;                                       //若成功返回其位置信息，否则返回0
    }

<img width="283" height="191" alt="image" src="https://github.com/user-attachments/assets/3d5ba0bf-c99c-4c49-b8df-af7b540ad2ee" />
<img width="284" height="160" alt="image" src="https://github.com/user-attachments/assets/69f97cbf-2279-4329-a719-7a512e8cef84" />  

## 折半查找（二分查找）
**前提：查找表必须是有序的**  
### 非递归算法
<img width="284" height="191" alt="image" src="https://github.com/user-attachments/assets/296aff17-005e-49fe-ae93-faf1ccbd131f" />  

***代码如下***  
*1.binary_search函数*

    #include<stdio.h>

    int binary_search(int* data,int len,int value){
        int low = 0;
        int high = len-1;
        int mid;
        while(low <= high){
            mid = (low + high) / 2;
            if(value > data[mid]){
                low = mid + 1;
            }
            else if(value < data[mid]){
                high = mid - 1;
            }
            else{
                return mid;
            }
        }
        return 0;
    }

*2.main函数同上*  
### 递归算法
<img width="265" height="188" alt="image" src="https://github.com/user-attachments/assets/66ff1aa7-8774-4170-806f-985d75fe4466" />  

*折半查找性能分析*  
<img width="283" height="205" alt="image" src="https://github.com/user-attachments/assets/0d96a741-129f-43ef-b3ba-7e9294c383c5" />
<img width="284" height="207" alt="image" src="https://github.com/user-attachments/assets/4063b637-2566-40e7-93a4-55912a3a1506" />
<img width="281" height="161" alt="image" src="https://github.com/user-attachments/assets/b670ab9f-1c68-4150-b581-18eedd928b1e" /> 

### Fibonacci查找（补充了解）
<img width="284" height="185" alt="image" src="https://github.com/user-attachments/assets/6ad150d1-53d9-4b9d-880e-3cc1504d1c52" />
<img width="283" height="196" alt="image" src="https://github.com/user-attachments/assets/83054313-bf5f-4ae4-b845-f0c08766f279" />
<img width="281" height="186" alt="image" src="https://github.com/user-attachments/assets/53e57b4d-7105-423d-99b8-303d6a6b05be" />
<img width="286" height="187" alt="image" src="https://github.com/user-attachments/assets/27107e9e-9a48-44f4-9fb1-e20cf323dd19" />

## 分块查找（块间有序，块内无序）
<img width="281" height="193" alt="image" src="https://github.com/user-attachments/assets/f9b8b1cb-c6a1-4a70-94d2-0169ace08546" />
<img width="283" height="185" alt="image" src="https://github.com/user-attachments/assets/17b7953c-d531-4b6f-9b9b-4e441690c204" />
<img width="283" height="200" alt="image" src="https://github.com/user-attachments/assets/94cd4e35-9aaa-4b98-9c01-097b78172516" />
<img width="284" height="151" alt="image" src="https://github.com/user-attachments/assets/150d5949-d727-401e-b592-80dbb1452676" />  

# 树表的查找
<img width="284" height="164" alt="image" src="https://github.com/user-attachments/assets/164d3ecd-737a-4fb3-b85e-b189e6a467c9" />  

## 二叉排序树
<img width="287" height="158" alt="image" src="https://github.com/user-attachments/assets/933c5e8c-52cd-4e1d-8be0-4a5401f575a5" />
<img width="431" height="193" alt="image" src="https://github.com/user-attachments/assets/1430b5ce-10b6-4e2f-b800-6d3c3e9c0c7d" />  

### 查找

***代码如下（和之前造二叉树的代码相似，但略有不同）***  
*1.造一颗二叉树*  

    #include<stdio.h>
    #include<stdlib.h>
    typedef int ElemType;     //定义二叉树节点数据类型，之前二叉树节点是字母，所以是char，此处为数字，故定义为int

    //定义二叉树节点结构体
    typedef struct TreeNode{          
        ElemType data;                //节点存储的数据（关键字）
        struct TreeNode *lchild;
        struct TreeNode *rchild;
    }TreeNode;

    typedef TreeNode* BiTree;         //定义二叉树指针类型，此定义含义：BiTree T等价于TreeNode *T,表示一个节点的指针

    //用数组模拟先序创建一棵二叉树（-1表示空节点）
    int treeArr[] = {70,55,49,30,-1,39,-1,-1,53,-1,-1,-1,80,75,-1,-1,98,95,-1,-1,-1};
    int idx = 0;

    //递归先序方式构建二叉树（含空节点）
    void createTree(BiTree *T){      //二级指针，若只使用一级指针即BiTree T,则无法改变T所指向的节点
        ElemType num;
        num = treeArr[idx++];        //取当前索引位置的值

        if(num == -1){               //-1代表空指针
            *T = NULL;
        }
        else{                        //创建新节点并递归构建左右子树
            *T = (BiTree)malloc(sizeof(TreeNode));
            (*T)->data = num;
            createTree(&(*T)->lchild);
            createTree(&(*T)->rchild);
        }
    }

    //先序遍历打印二叉树结点值（调式用）
    void preOrder(BiTree T){
        if(T == NULL){
            return;
        }
        printf("%d ",T->data);
        preOrder(T->lchild);
        preOrder(T->rchild);
    }

*2.查找函数*  

    //T:当前子树根节点   value:要查找的值    parent:当前节点的父节点    pos:指针变量，用于返回找到的节点
    int search_bst(BiTree T,int value,BiTree parent,BiTree *pos){
        if(T == NULL){
            *pos = parent;     //递归到空节点，查找失败，将父节点返回（后面内容可用于插入）
            return 0;
        }

        if(T->data == value){
            *pos = T;
            return 1;
        }

        if(T->data > value){
            return search_bst(T->lchild,value,T,pos);
        }
        else{
            return search_bst(T->rchild,value,T,pos);
        }
    }   

*3.main函数*  

    int main(int argc,char const *argv[]){
        BiTree T;
        createTree(&T);

        BiTree searchT;
        search_bst(T,53,NULL,&searchT);     //查找值为53的节点

        preOrder(T);       //输出树结构（调试用）
        printf("\n");

        printf("%d\n",seachT->data);     //输出查找到的节点值
        return 0;
    }

## 插入
<img width="283" height="176" alt="image" src="https://github.com/user-attachments/assets/19608760-65bd-4e25-a52f-e54924612b11" />
<img width="284" height="184" alt="image" src="https://github.com/user-attachments/assets/27eb6f7e-cf47-4193-a8ab-bce6fa283d19" />

***代码如下***  
*直接写插入的代码*  

    int insert_bst(BiTree *T,int value){
        BiTree parent,pos;           //parent用于辅助search_bst, pos表示插入位置或已存在的位置
        BiTree curr;

        //查找插入的位置
        int status = search_bst(*T,value,NULL,&pos);
        if(status == 0){
            //如果未找到，创建新结点
            curr = (BiTree)malloc(sizeof(TreeNode));
            curr->data = value;
            curr->lchild = NULL;
            curr->rchild = NULL;
            
            //如果树为空，新结点作为根结点
            if(pos == NULL){
                *T = curr;
            }
            
            //插入为左子结点
            else if(value < pos->data){
                pos->lchild = curr;
            }
            
            //插入为右子结点
            else {
                pos->rchild = curr;
            }
            return 1;
        }
        else{               //已存在，不插入
            return 0;
        }
    }

### 删除
<img width="283" height="198" alt="image" src="https://github.com/user-attachments/assets/367d8e9b-081f-4b6a-8792-f90948e150e1" />
<img width="272" height="195" alt="image" src="https://github.com/user-attachments/assets/a49e0baf-6391-4202-b889-da13845b6265" />  
<img width="344" height="190" alt="image" src="https://github.com/user-attachments/assets/c0d2be18-eda5-4099-9d1f-37310f505b19" />

*b站上的删除操作与老师讲的略有不同，老师讲的是为了不改变删除后二叉树的中序遍历的序列，用其直接前驱或直接后继替补；b站是从二叉树定义出发，用其左子树最大值或右子树最小值替补*  

***代码如下***  
*直接写删除的代码*  

    //真正执行删除的操作（分三种情况）
    int delete(BiTree *d){
        BiTree temp,record;
        //情况1：只有左子树或无子节点
        if((*d)->rchild == NULL){
            temp = *d;
            *d = (*d)->lchild;
            free(temp);
        }
        
        //情况2：只有右子树
        else if((*d)->lchild == NULL){
            temp = *d;
            *d = (*d)->rchild;
            free(temp);
        }

        //情况3：左右子树都有，找左子树中最大的节点替换
        else{
            temp == *d;
            record = (*d)->lchild;
            while(record->rchild !=NULL){
                temp = record;
                record = record->rchild;
            }
            (*d)->data = record->data;       //替换当前值
            
            if(temp != *d){                 //删除替代节点
                temp->rchild = record->lchild;
            }
            else{
                temp->lchild = record->lchild;
            }
            free(record);
        }
        return 1;
    }
            

    //递归查找并删除目标节点
    int delete_bst(BiTree *T,int value){
        if(*T == NULL){
            printf("not found!\n");
            return 0;
        }
        else{
            if((*T)->data == value){
                return delete(T);
            }
            else if((*T)->data > value){
                return delete_bst(&(*T)->lchild,value);
            }
            else{
                return delete_bst(&(*T)->rchild,value);
            }
        }
    }

    
            

            
        







          
            



            
              
